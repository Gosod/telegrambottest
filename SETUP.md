# 🚀 Production версия бота — Инструкция запуска

## 📦 Файлы:

```
production-v2/
├── bot.py        ← Бот (Python)
└── index.html    ← Web App (всё в одном файле)
```

---

## ⚡ Шаг 1: GitHub Pages (Web App)

1. Загрузи `index.html` в репозиторий `telegram-bot-webapp`
2. Замени старый файл
3. Подожди 1-2 минуты — GitHub обновит страницу

**Проверь в браузере:**
```
https://твой-username.github.io/telegram-bot-webapp/
```

---

## ⚙️ Шаг 2: Настрой bot.py

Открой `bot.py` и измени:

```python
TOKEN = os.environ.get('TELEGRAM_BOT_TOKEN', '')
WEBAPP_URL = 'https://твой-username.github.io/telegram-bot-webapp/'

ADMIN_IDS = [
    699229724,   # mchsman
    924261386,   # eugeneoldyard
]
```

---

## 🧪 Шаг 3: Тест локально

```powershell
cd C:\Users\Photomechanics\Desktop\telegabot

# Скопируй bot.py в папку проекта
# Запусти с токеном тестового бота
$env:TELEGRAM_BOT_TOKEN="8514288248:AAG-OZ02ePaK1XbM5CXwC7OkCqiYBCYE_pw"
python bot.py
```

**В Telegram:**
1. Найди тестового бота
2. `/start`
3. Нажми **"📱 Открыть приложение"**
4. Тестируй все функции!

---

## ✅ Что тестировать:

### Обычный пользователь:
- [ ] Открывается Web App
- [ ] Выбор проекта (карточки)
- [ ] Слайдер и быстрые кнопки часов
- [ ] Добавление нескольких проектов в один отчёт
- [ ] Комментарий
- [ ] Отправка → подтверждение в боте
- [ ] Статистика → показывает данные

### Администратор:
- [ ] Видны admin-кнопки в меню
- [ ] Кнопка "➕ Добавить проект" в форме отчёта
- [ ] Вкладка "Обзор" — цифры и график
- [ ] Вкладка "Сотрудники" — список с деталями
- [ ] Вкладка "Проекты" — цифры и диаграмма
- [ ] Вкладка "Отчёты" — список последних
- [ ] Вкладка "Управление" — проекты + назначение

---

## 🚀 Шаг 4: Деплой на сервер

После успешного теста:

```powershell
# Копируй bot.py на сервер
scp bot.py vpsbg:~/telegram_bot.py
```

**На сервере:**
```bash
ssh vpsbg

# Останови текущего бота
sudo systemctl stop telegram-bot

# Задай правильный токен РАБОЧЕГО бота
sudo nano /etc/systemd/system/telegram-bot.service
# Измени токен в Environment=

# Перезапусти
sudo systemctl daemon-reload
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

---

## 📋 Команды в боте (минимально):

| Команда | Кто | Что делает |
|---------|-----|-----------|
| `/start` | Все | Открывает Web App |
| `/export` | Админ | CSV выгрузка |
| `/notify` | Админ | Напомнить всем |

---

## 🔔 Напоминания:

- Автоматически в **16:50 МСК (пн-пт)**
- Только тем кто **не заполнил отчёт сегодня**
- Настраивается в `bot.py`:
```python
REMINDER_HOUR = 16
REMINDER_MINUTE = 50
REMINDER_DAYS = (0, 1, 2, 3, 4)  # Пн-Пт
```

---

## 📱 Что видит пользователь:

### Главная:
```
[📝 Новый отчёт]  [📊 Статистика]
[👑 Админ панель]  ← только для админа
```

### Форма отчёта:
```
📁 Проекты: [РС][МРК][КП][ДЗ]...
[➕ Добавить проект] ← только для админа

⏱️ Часов: 6
[●────────]
[2ч][4ч][6ч][8ч]

[➕ Добавить ещё проект]  ← мультипроект!

💬 Комментарий...

📋 Сводка:
  РС — 4 ч
  МРК — 2 ч
  Итого: 6 ч

[📤 Отправить отчёт]
```

---

## 🎯 Ключевые отличия от предыдущей версии:

| Было | Стало |
|------|-------|
| Много команд (/report, /stats...) | Одна кнопка "📱 Открыть" |
| Только один проект в отчёт | Несколько проектов в один отчёт |
| Статистика текстом | Графики и таблицы |
| Управление через команды | Всё через Web App |
| Нет кнопки "Назад" | Кнопка "Назад" везде |

---

**Готово! Тестируй и пиши результат! 🚀**
